query getIrosById($iroIds: [BigInt!]) {
  iros(where: { iroId_in: $iroIds }) {
    iroId
    status
    unitPrice
    currency
    currencyDecimals
    targetFunding
    start
    end
    totalFunding
  }
}

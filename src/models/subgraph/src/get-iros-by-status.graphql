query getIrosByStatus($statuses: [Status!]) {
  iros(where: { status_in: $statuses }) {
    iroId
    status
    unitPrice
    currency
    currencyDecimals
    softCap
    hardCap
    start
    end
    totalFunding
  }
}
